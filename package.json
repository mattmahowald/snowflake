{
  "name": "eng-growth",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "https://github.com/Medium/eng-growth",
  "license": "MIT",
  "dependencies": {
    "d3": "^4.11.0",
    "isomorphic-unfetch": "^3.0.0",
    "next": "^4.0.0-beta.4",
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  },
  "scripts": {
    "dev": "node buildTracks.js && next",
    "export": "node buildTracks.js && next build && next export",
    "flow": "flow",
    "deploy": "gh-pages -t -d out"
  },
  "devDependencies": {
    "csvtojson": "^2.0.8",
    "flow-bin": "^0.57.2",
    "gh-pages": "^2.0.1"
  }
}
